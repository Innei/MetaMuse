export enum CommentReplyMailType {
  Owner = 'owner',
  Guest = 'guest',
}

export enum CommentRefTypes {
  Post = 'Post',
  Note = 'Note',
  Page = 'Page',
  Recently = 'Recently',
}
export const enum CommentSenderType {
  Owner = 'owner',
  Guest = 'guest',
}
